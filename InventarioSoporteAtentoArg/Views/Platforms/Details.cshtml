@model InventarioSoporteAtentoArg.Models.Platform

@{
    ViewBag.Title = "Detalles";
}

<h2>Detalles</h2>

<div>
    <h4>Plataforma</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.PlatformID }, new { @class = "btn btn-primary btn-sm" }) |
    @Html.ActionLink("Volver", "Index", new { }, new { @class = "btn btn-default btn-sm" })
</p>
